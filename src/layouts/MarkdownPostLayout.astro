---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title}>
  <p>{frontmatter.publicationDate.toString().slice(0, 10)}</p>
  <p><em>{frontmatter.description}</em></p>

  <article>
    <slot />
  </article>
</BaseLayout>
